{
  "name": "apollo-server",
  "version": "1.0.0",
  "description": "### What is GraphQL? GraphQL is the query language for fetchting resources over HTTP/HTTPs. It mainly used GET / POST methods. It is having single endpoint and provide only requested data. It support mutation also (update). It allows client to define the structure of the data required and the same structure of the data is returned from server. GraphQL consist of type system, query language, and execution semantics, static validations and type introspection.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run nodemon",
    "nodemon": "npx nodemon",
    "lint": "eslint ./src",
    "lint:fix": "npm run lint --fix"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/permil-st/apollo-server.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/permil-st/apollo-server/issues"
  },
  "homepage": "https://github.com/permil-st/apollo-server#readme",
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint",
      "pre-push": "npm run lint"
    }
  },
  "dependencies": {
    "apollo-datasource-rest": "^0.9.2",
    "apollo-server-express": "^2.14.2",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "graphql": "^15.1.0",
    "helmet": "^3.22.0",
    "merge-graphql-schemas": "^1.7.8"
  },
  "devDependencies": {
    "@babel/core": "^7.10.2",
    "@babel/node": "^7.10.1",
    "@babel/preset-env": "^7.10.2",
    "commitizen": "^4.1.2",
    "cz-emoji": "^1.2.1",
    "dotenv": "^8.2.0",
    "eslint": "^6.8.0",
    "eslint-config-airbnb-base": "^14.1.0",
    "eslint-plugin-import": "^2.21.1",
    "git-cz": "^4.6.2",
    "husky": "^4.2.5",
    "nodemon": "^2.0.4"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/git-cz"
    }
  }
}
